---
interface Props { size?: number; color?: string; accent?: string; label?: string; }
const { size = 32, label } = Astro.props;
---
<img src="/brand/hyphos-mark.svg" width={size} height={size} alt={label || ''} aria-hidden={label ? undefined : 'true'} />
